export interface IProject {
  title: string;
  image: string;
  id: number | string;
}

export interface ICommentPair {
  title: string;
  text: string;
  image: string;
  id: number | string;
}

export interface IHeroSlider {
  title: string;
  text: string;
  id: number;
}
